/**
 * @typedef {Object} VkTeamsClient
 * @property {(params: { chatId: string, userId: string }) => Promise<{ isAdmin: boolean }>} getChatMemberRole
 * @property {(params: { chatId: string, text: string }) => Promise<void>} sendMessage
 */

module.exports = {};
